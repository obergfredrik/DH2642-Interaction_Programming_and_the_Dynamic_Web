import AlertTemplate from 'react-alert-template-basic'

const MessageBoxTemplateWrapper = function (_ref) {
    _ref.style.textTransform = "initial";
    return AlertTemplate(_ref);
};

export default MessageBoxTemplateWrapper;